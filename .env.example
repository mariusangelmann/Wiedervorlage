# Email Server Configuration
IMAP_SERVER=imap.example.com
IMAP_PORT=993
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
EMAIL_USERNAME=remind@example.com
BASE_DOMAIN=example.com

# Authentication Method (password or oauth2)
AUTH_METHOD=password

# Password Authentication (when AUTH_METHOD=password)
EMAIL_PASSWORD=your_password

# OAuth2 Authentication (when AUTH_METHOD=oauth2)
# OAUTH2_CLIENT_ID=your-client-id
# OAUTH2_CLIENT_SECRET=your-client-secret
# OAUTH2_REFRESH_TOKEN=your-refresh-token
# OAUTH2_ACCESS_TOKEN=your-access-token (optional)
# OAUTH2_ACCESS_URL=https://oauth2.provider.com/token (optional)
# OAUTH2_PROVIDER=google (google/microsoft/yahoo/custom)

# Service Settings
CHECK_INTERVAL=60
DEBUG_MODE=false
MAX_RETRIES=3
REMINDERS_FILE=reminders.json

# Language Settings
# Language code (en or de)
LANGUAGE=en
# Optional: Path to custom translations file (JSON)
# CUSTOM_TRANSLATIONS_PATH=translations/custom.json

# Heartbeat Settings (Optional)
# Enable/disable heartbeat monitoring (default: false)
HEARTBEAT_ENABLED=false
# BetterStack heartbeat URL
# HEARTBEAT_URL=https://uptime.betterstack.com/api/v1/heartbeat/XXXXXXXXXXXXXXXXXXXXXX
# Heartbeat interval in seconds (default: same as CHECK_INTERVAL)
# HEARTBEAT_INTERVAL=60
